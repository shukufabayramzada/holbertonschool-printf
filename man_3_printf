_printf

NAME
_printf - Print formatted output

SYNOPSIS
#include "main.h"

int _printf(const char *format, ...);

DESCRIPTION
The _printf function writes the output according to the format specified by the format string.

The format string may contain format specifiers, which begin with the percent character '%', followed by a conversion specifier.

The conversion specifiers supported by _printf are:

%d, %i    Prints a signed integer.

%s        Prints a null-terminated string.

%c        Prints a character.

%%        Prints a percent character '%'.

RETURN VALUE
The _printf function returns the number of characters printed on success or a negative value on error.

EXAMPLES
Example 1: Printing an integer:
	int num = 42;
	_printf("The number is: %d\n", num);

Example 2: Printing a string:
	char str[] = "Hello, world!";
	_printf("%s\n", str);

Example 3: Printing a character:
	char ch = 'A';
	_printf("Character: %c\n", ch);

Example 4: Printing a percent character:
	_printf("This is a percent sign: %%\n");

AUTHORS
	Shukufa Bayramzada
	Sayyara Yusupova
